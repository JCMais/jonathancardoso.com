overwrite: true
schema: graphql/schema.graphql
hooks:
  afterOneFileWrite:
    - prettier --write
config:
  omitOperationSuffix: true
documents:
  - ./node_modules/gatsby-*/**/*.js
generates:
  gatsby/types/graphql.ts:
    documents:
      - ./gatsby/**/*.{tsx,ts}
    watch:
      - ./gatsby/**/*.{tsx,ts}
    plugins:
      - 'typescript'
      - 'typescript-operations'
